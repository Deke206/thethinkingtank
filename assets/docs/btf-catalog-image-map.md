# BTF catalog image mapping

The 41 WebP assets under `assets/catalog/btf/` are mapped to the corresponding product group shown in the quotation PDFs. Every individual quotation row inherits the image for its source product group while keeping its own row number, model, length, voltage, density, width and protection details.
