# SIM Auto production assets

- `car-base-1.txt` through `car-base-6.txt` reconstruct the approved WebP sedan artwork in the browser.
- `alignment-v1.json` preserves the user-adjusted SVG placement source.
- The live page imports `css/bike-builder.css` so the auto vectors use the same RGB gradient, pixel dash pattern, twinkle animation and multicolor glow as LED SIM BIKE.
- Rollback branch: `rollback/sim-auto-placeholder-20260729`.
