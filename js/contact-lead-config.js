(() => {
  "use strict";

  window.ShynetymeLeadConfig = Object.freeze({
    // Paste the deployed Google Apps Script /exec URL here before merging this branch.
    endpoint: "",
    fallbackEmail: "westsidelistingservices@gmail.com",
    timeoutMs: 25000
  });
})();
